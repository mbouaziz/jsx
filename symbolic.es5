{
  global["symbol" = function(s){return prim("symbol",s)}];
// TODO: cast content of assume to boolean
  global["assume" = function(e){return prim("assume",e)}];
  global["assume_primitive" = function(e){return prim("assume",prim("primitive?",e))}];
  global["assume_callable" = function(e){return prim("assume",prim("is-callable",e))}];
  global["assume_string" = function(e){return prim("assume",prim("primitive?",e)); prim("assume",prim("stx=",prim("typeof",e),"string"))}]
}
